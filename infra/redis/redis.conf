# Local/dev Redis for redis-pulse
# Override via docker-compose command or mount.

bind 0.0.0.0
protected-mode no
port 6379
tcp-keepalive 300

# Persistence (optional; cache can be ephemeral)
appendonly yes
appendfsync everysec

# Guardrails
maxmemory 256mb
maxmemory-policy allkeys-lru
